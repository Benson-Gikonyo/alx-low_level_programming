#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c : if c is lowercase, return 1, else
 * return 0
 * Return: 0 or 1.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
